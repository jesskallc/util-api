{
    "name": "util-api",
    "version": "1.0.0",
    "description": "package for creating workers templates",
    "main": "index.js",
    "scripts": {
        "develop": "wrangler dev",
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier --write '**/*.{js,css,json,md}'"
    },
    "author": "jesskallc <rallmond@jesska.dev>",
    "license": "ISC",
    "devDependencies": {
        "prettier": "^1.17.0"
    },
    "dependencies": {
        "sanitize-html": "^2.3.0",
        "serverless-cloudflare-workers": "^1.2.0"
    }
}
